import { NumberFormatDuDirective } from './number-format-du.directive';

describe('NumberFormatDuDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberFormatDuDirective();
    expect(directive).toBeTruthy();
  });
});
